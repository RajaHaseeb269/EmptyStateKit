Tiny UIKit helpers to show a friendly “empty state” in `UITableView` and `UICollectionView`.

File → Add Packages… → https://github.com/<your-username>/EmptyStateKit

Open `Examples/EmptyStateDemo` and run.
